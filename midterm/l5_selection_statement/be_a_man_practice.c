#include <stdio.h>

int main(){
	int birth, height, weight, age, bmi;
	char *phy, *mili;
	
	scanf("%d%d%d", birth, high, weight);
	age = 2021 - birth;
	bmi = weight / pow(height / 100, 2);
	if(age < 19 || age > 36){
		phy = 
		mili =  "No need to perform Military Service"
	}
	else{
		if(bmi >= 17 && bmi <= 31){
			if(birth < 1993){ 
				phy = 
				mili = 
			}
			else{
			}
		}
		else if(bmi >= 16.5 && bmi <= 17 || bmi >= 31 bmi <= 31.5)
			if(birth < 1993){
				phy = 
				mili = 
			}
			else{
			}
		}
		else{
			phy = 
			mili = 
		}
	}

	return 0;
}
